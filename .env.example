# Credenciales de Postgres para docker compose.
# Tienen que coincidir con la DATABASE_URL de backend/.env, por eso vienen
# completas: copiá este archivo como .env y levantá, sin cambiar nada.
POSTGRES_USER=usuario
POSTGRES_PASSWORD=password
POSTGRES_DB=colppy_ventas
